########################################
# SUPABASE CONFIGURATION
########################################

# URL of your Supabase project
SUPABASE_URL="https://your-project-id.supabase.co"

# Public (safe) key for frontend usage
# NEVER use this in backend-only logic
SUPABASE_ANON_KEY="your-anon-key-here"

# Private full-access key for backend servers (Cloud Run, scripts)
# NEVER expose this on frontend or commit real values
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"



########################################
# BACKEND CONFIGURATION
########################################

# Port for local development
PORT=8080

# Node environment (development | production)
NODE_ENV="development"

# If using scheduler or cron verification
CRON_SECRET="your-cron-secret-here"

# If backend needs to know which environment it's running in
APP_ENV="local"



########################################
# GCP / GOOGLE CLOUD (if applicable)
########################################

# Google Cloud project ID where the backend is deployed
GCP_PROJECT_ID="your-gcp-project-id"

# Region for Cloud Run / GCP services
GCP_REGION="us-central1"

# Optional: credentials for local dev impersonation
GOOGLE_APPLICATION_CREDENTIALS="./gcp-service-account.json"



########################################
# FIREBASE AUTH (ONLY IF KEEPING FIREBASE)
########################################

# If keeping Firebase Authentication for sign-in
# (Otherwise, remove this section)
FIREBASE_API_KEY="your-firebase-web-api-key"
FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
FIREBASE_PROJECT_ID="your-firebase-project-id"



########################################
# SCRAPER CONFIG (IF APPLICABLE)
########################################

# Proxy providers, scraping secrets, API tokens, etc.
SCRAPER_PROXY_URL=""
SCRAPER_PROXY_KEY=""
SCRAPER_USER_AGENT="Mozilla/5.0"

# How many threads or batches you want locally
SCRAPER_BATCH_SIZE=10
